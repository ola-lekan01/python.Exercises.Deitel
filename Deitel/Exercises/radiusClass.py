import math

radius = float(input("Enter radius: ")); height = float(input("Enter Height: "));
volume = math.pi * (radius ** 2) * height;
print("The Volume of the Cyclinder with radius", radius, "and height" ,  height, "is", volume)